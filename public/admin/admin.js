document.getElementById('f').addEventListener('submit',async e=>{e.preventDefault();const file=document.getElementById('csv').files[0],fd=new FormData();fd.append('file',file);const rep=document.getElementById('replace').checked;const r=await fetch(`/api/import/articles?replaceStoreCatalog=${rep}`,{method:'POST',body:fd});const p=await r.json();document.getElementById('out').textContent=JSON.stringify(p,null,2)})
