# ZoneMind 0.9.5 — Synchronization & Conflicts

- endpoint idempotente `/api/mapper/sync-operation`;
- registro server delle operazioni completate;
- sincronizzazione cronologica della coda IndexedDB;
- riconoscimento dei conflitti HTTP 409;
- dialogo di risoluzione con scelta tra stato server e operazione offline;
- sincronizzazione automatica al ripristino della connessione;
- indicatori separati per operazioni pendenti e conflitti.
